<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Blog</title>
  <link rel="stylesheet" href="/styles/main.css">
</head>

<body>
  <div class="container">
    <h1>
      <%= heading %>
    </h1>

    <% if (locals.dettaglio) { 

      %>
      <form id="editForm" method="post" action="/edit/<%=dettaglio.id%>">
        <input type="text" name="title" value="<%=dettaglio.titolo %>">
        <textarea name="content" id="content" rows="10"><%=dettaglio.testo %></textarea>
        <input type="text" name="punti" value="<%=dettaglio.punteggio %>">
        <button class="full-width" type="submit">
          <%= submit %>
        </button>
      </form>
      <% } else {%>
        <form id="newPostForm" method="post" action="/new">
          <input type="text" name="title" placeholder="Title" required>
          <textarea name="content" placeholder="Content" required rows="10"></textarea>
          <input type="text" name="punti" placeholder="Punti" required>
          <button class="full-width" type="submit">
            <%= submit %>
          </button>
        </form>
        <% } %>

  </div>
</body>

</html>